import 'package:flutter/material.dart';

class EdgeWidget extends StatelessWidget {
  const EdgeWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
